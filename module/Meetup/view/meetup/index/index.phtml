<h1>Meetup</h1>

<div class="row">
    <div class="col-md-12">
        <a href="<?= $this->url('meetup/add'); ?>" class="btn btn-primary">Add new</a>
    </div>

    <div class="col-md-12">
        <?php foreach ($this->meetups as $meetup): /* @var $meetups Meetup\Controller\IndexController */ ?>
            <div class="col-md-4">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title">
                            <a href="<?= $this->url('meetup/add') ."/". $meetup->getId(); ?>">
                                <?= $meetup->getTitle() ?>
                            </a>

                        </h3>
                    </div>
                    <div class="panel-body">
                        <p>
                            Date de début : <?= $meetup->getStartDate() ?>
                        </p>
                        <p>
                            Date de fin : <?= $meetup->getEndDate() ?>
                        </p>
                        <p>
                            Organisé par : <?= $meetup->getOrganisateur() ?>
                        </p>
                        <p>
                            Entreprise : <?= $meetup->getEntreprise() ?>
                        </p>
                        <p>
                            Nombre de participant : <?= $meetup->getParticipant() ?>
                        </p>
                        <p>
                            Description : <br>
                            <fieldset>
                                <?= $meetup->getDescription() ?>
                            </fieldset>

                        </p>
                        <p>
                            <a href="<?= $this->url('meetup/edit', ['id' => $meetup->getId()]); ?>"> edit</a>

                            <form action="<?php echo $this->url('meetup/delete'); ?>" method="post">
                                <input type="hidden" name="id" value="<?= $meetup->getId(); ?>">
                                <input type="submit" value="Delete">
                            </form>
                        </p>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</div>
